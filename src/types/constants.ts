// If you run into rate limits on api.github.com in client-only contexts,
// you can switch to RAW mode which usually has friendlier CDN caching.
// Keep RAW true for simplicity.
export const USE_RAW = true;
export const DEFAULT_OWNER = "Storage-Tech-2";
export const DEFAULT_REPO = "Archive";
export const DEFAULT_BRANCH = "main";